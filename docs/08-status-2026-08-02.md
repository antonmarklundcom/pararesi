# 08 — Status Refresh (2026-08-02)

Re-verification of `docs/07-review-and-next-steps.md` (written 2026-07-24) against the
code currently on `main`. **Nothing has been merged since PR #5**, so doc 07 remains the
active work plan. It does not need rewriting — only the corrections below.

## Corrections to doc 07

- **Section A, README bullet is now stale.** PR #5 already fixed it; `README.md:8` reads
  *"Status (2026-07-24): phases 0–6 built, `npm run build` green."* Drop that bullet.

Every other item in doc 07 was re-checked in the source and still stands:

| Item | Re-verified |
|------|-------------|
| B1 idempotency key kills renewals | ✅ `src/app/api/webhooks/lemonsqueezy/route.ts:51` still `${eventName}:${resourceId}` |
| B2 fixtures encode wrong shape | ✅ `fixtures/subscription_payment_success.json` still `"type": "subscriptions"`, no `updated_at`, no invoice id |
| B3 handler failures invisible | ✅ route still returns 200 on handler throw; no admin webhook surface |
| B4 no tests, no CI | ✅ zero test files, no `.github/workflows` |
| A — guide price / legal placeholders | ✅ `[PLACEHOLDER]` in `/guide` (×2), `/pricing`, `/about`, `/terms`, `/privacy`, `/refund-policy` |
| A — phases 7, 8, 9 | ✅ not started; no `docs/qa-report-phase7.md` |
| C1 pricing ladder inverted | ✅ unchanged — doc 05 still $7/mo insider incl. everything, guide TBD |

## Plan-level note

The build phases are done; what remains is **one code fix + one decision + deploy**, and
the decision (C1 guide price / ladder) is the only true blocker with no code path around
it. Doc 01's phase table should be read as: phases 0–6 complete, phase 7 pending and now
expanded to include B1/B2/B4, phases 8–9 blocked on owner-provided accounts and content.

Recommended order is unchanged from doc 07 §"Recommended order of work".
